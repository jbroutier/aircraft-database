<div class="w-100">

  {{ form_start(form) }}

  <div class="input-group">

    {{ form_widget(form.query) }}

    <button type="submit" class="btn btn-primary">
      {{ 'Search'|trans }}
    </button>
  </div>

  {{ form_rest(form) }}
  {{ form_end(form) }}

</div>
